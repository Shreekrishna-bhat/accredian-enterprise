'use client';
import {useState} from 'react';
const faqs=[['Can programs be customized for our organization?','Yes. We customize learning journeys around business priorities, target roles, existing skill levels, cohort size and delivery preferences.'],['What formats do you support?','We support live virtual, in-person and blended formats, with projects and practice built into the learning journey.'],['How do you measure impact?','We combine participation and completion data with assessments, project outcomes and learner/application feedback.'],['Can you run programs for multiple locations?','Yes. Cohorts can be designed for distributed teams while maintaining a consistent curriculum and measurement approach.'],['How quickly can a program launch?','A typical discovery-to-launch cycle depends on scope, but we can rapidly prototype a pathway and refine it with stakeholders.']];
export default function FAQ(){const [open,setOpen]=useState(0);return <section id="faq" className="section faq"><div className="faq-head"><p className="section-kicker">FAQ</p><h2>Questions, answered.</h2></div><div>{faqs.map(([q,a],i)=><div className="faq-item" key={q}><button onClick={()=>setOpen(open===i?-1:i)}><span>{q}</span><b>{open===i?'−':'+'}</b></button>{open===i&&<p>{a}</p>}</div>)}</div></section>}
