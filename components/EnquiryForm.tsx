'use client';
import {FormEvent, useState} from 'react';
export default function EnquiryForm(){const [status,setStatus]=useState('');const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setStatus('Sending…');const form=e.currentTarget;const data=Object.fromEntries(new FormData(form));const r=await fetch('/api/enquiry',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();setStatus(j.message);if(r.ok)form.reset()};return <section id="enquiry" className="section enquiry"><div className="enquiry-copy"><p className="section-kicker">LET'S TALK</p><h2>Build the capability your strategy needs.</h2><p>Tell us what you are trying to solve. Our enterprise team will help shape the right learning pathway.</p><div className="contact-points"><span>✓ Customized pathways</span><span>✓ Expert-led delivery</span><span>✓ Measurable outcomes</span></div></div><form className="form-card" onSubmit={submit}><label>Name<input name="name" required placeholder="Your name"/></label><label>Work email<input name="email" type="email" required placeholder="you@company.com"/></label><label>Company<input name="company" required placeholder="Company name"/></label><label>What do you need?<select name="requirement" required defaultValue=""><option value="" disabled>Select an area</option><option>AI & Data</option><option>Leadership</option><option>Product</option><option>Technology</option><option>Other</option></select></label><button className="btn primary full" type="submit">Send enquiry →</button>{status&&<p className="form-status">{status}</p>}</form></section>}
